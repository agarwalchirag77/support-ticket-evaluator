# Zendesk credentials
ZENDESK_EMAIL=you@hevodata.com
ZENDESK_API_TOKEN=your_zendesk_api_token_here

# Zendesk group IDs (find via: GET /api/v2/groups.json)
CHAT_L1_GROUP_ID=123456789
GENERAL_ESCALATION_GROUP_ID=987654321

# LLM API keys (only the one you use needs to be set)
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# Notification: email-to-Slack channel
# Find Slack channel email: Channel > Settings > Integrations > "Send emails to this channel"
NOTIFICATION_EMAIL=alerts@hevodata.com
NOTIFICATION_EMAIL_PASSWORD=your_app_password_here
SLACK_CHANNEL_EMAIL=abc123xyz@hevodata.slack.com

# Optional: Slack webhook (alternative to email-to-Slack)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# Snowflake (only when storage.backend=snowflake, e.g. on the remote VM)
# On the server, restrict this file: chmod 600 .env
SNOWFLAKE_ACCOUNT=your_account_locator      # e.g. ab12345.us-east-1
SNOWFLAKE_USER=ticket_evaluator_svc
SNOWFLAKE_PASSWORD=your_password_here
SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_DATABASE=SUPPORT_ANALYTICS
SNOWFLAKE_SCHEMA=ZENDESK_AUDIT
SNOWFLAKE_ROLE=TICKET_EVALUATOR

# Read-only reader creds — ONLY for the Cowork agent-feedback skill's environment
# (deploy/snowflake_reader.sql creates this SELECT-only user/role). When both are set,
# the app connects as the reader (reusing the account/warehouse/database/schema above)
# instead of the pipeline user. Leave UNSET on the pipeline VM.
SNOWFLAKE_READER_USER=TICKET_EVALUATOR_READER_USER
SNOWFLAKE_READER_PASSWORD=your_reader_password_here
# SNOWFLAKE_READER_ROLE=TICKET_EVALUATOR_READER   # optional; defaults to TICKET_EVALUATOR_READER
